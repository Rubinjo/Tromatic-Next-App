class User {
	constructor(id, companyId, role, email, fullName, lastActivity) {
		this.id = id;
		this.companyId = companyId;
		this.role = role;
		this.email = email;
		this.fullName = fullName;
		this.lastActivity = lastActivity;
	}
}

export default User;
