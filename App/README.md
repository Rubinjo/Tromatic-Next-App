## Setup Dev Environment

1. Install all node modules with the `npm install` command
2. Create `.env` file in the `App` directory.
3. Add the following firebase api key details:
    - APIKEY
    - AUTHDOMAIN
    - DATABASEURL
    - PROJECTID
    - STORAGEBUCKET
    - MESSAGINGSENDERID
    - APPID